.video-wrapper.relative
  %span.bold.copy-color X
  = video_tag @src || '#', controls: true, class: 'video', autoplay: true

:javascript
  $('.video-wrapper').click(function() { $(this).remove() })
  $('video').click(function(e) { e.stopPropagation() })
